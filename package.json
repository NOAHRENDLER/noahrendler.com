{
  "name": "soc-portfolio",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint js/",
    "check": "npm run lint && npm run test",
    "dev": "npx --yes serve --listen 8080 --no-clipboard ."
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@vitest/coverage-v8": "^3.0.0",
    "eslint": "^9.0.0",
    "eslint-plugin-security": "^3.0.0",
    "globals": "^15.0.0",
    "jsdom": "^25.0.0",
    "vitest": "^3.0.0"
  }
}
